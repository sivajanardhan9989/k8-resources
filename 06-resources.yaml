apiVersion: v1
kind: Pod 
metadata: 
   name: resources
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        cpu: "100m" # 1000m is 1cpu
        memory: "128Mi"
      limits: 
        cpu: "150m"
        memory: "256Mi"

     