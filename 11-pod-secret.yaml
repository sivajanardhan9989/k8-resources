apiVersion: v1
kind: Pod
metadata:
  name: pod-secret # pod name
  labels:
    project: roboshop
    environment: dev
    purpose: poc
spec:
  containers:
  - name: nginx
    image: nginx   
    envFrom:
    - secretRef:
        name: nginx-secret